---
layout: post
title: EM算法的直觉推导
date: 2023-3-30 12:53:24.000000000 +09:00
tags: math
---

EM 算法（Expectation-Maximization algorithm）是一种最大期望算法。主要包括 E-step 和 M-step 两部分，通过反复迭代来获得相关参数 $\theta$ 的估计。

EM 算法的核心，主要包括两个函数，一个被称为完全数据的对数似然，公式表达为：

$$
\log{L(X, Z, \theta)} = \log{\mathrm{P}(X, Z|\theta)}
$$

其中 $X$ 为观察数据，$Z$ 为隐参数而 $\theta$ 为模型参数。

另一个参数用来描述完全数据的对数似然期望，公式表达为：

$$
\begin{aligned}
Q(\theta | \theta\_{old}) & = \mathrm{P}(Z|X, \theta)\cdot \log{L(X,Z,\theta)} \\ 
& = \sum\_{Z}{\mathrm{P}{Z|X, \theta\_{old}}} \cdot \log{L(X, Z, \theta)}
\end{aligned}
$$

这样就可以通过研究 $Q$ 函数来最小化 $\theta$ 参数，进而不断迭代 $\theta$ 的值。


例如对于较为常见的高斯混合模型，可以通过求偏导为零的方法，分别更新 $\theta$ 参数中包括的期望 $\miu$ 、标准差 $\sigma$ 以及混合系数 $\pi$ 。



